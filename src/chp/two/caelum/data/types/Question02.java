package chp.two.caelum.data.types;

/**
 * @author Mariana Azevedo
 * @since 07/04/2019
 * 
 * Question 1.2.2 (page 60) - Choose the appropriate option when attempting to compile 
 * and run the following code:
 * 
 * public static void main(String[] args) {
 *	int
 *	age
 *	= 100;
 *	System.out.println(age);
 * }
 * 
 * a) Does not compile: an error in the line that tries to access the variable age. (x)
 * b) Compiles and prints 0, or the age that is passed on the command line.
 * c) Compiles and prints the age that is passed on the command line.
 * d) Compiles and prints "???", or prints the age.
 */
public class Question02 {

	public static void main(String[] args) {
		//int age;
		//if(args.length > 0) {
			//age = Integer.parseInt(args[0]);
		//}else {
			//System.err.println("???");
		//}
		
		//System.out.println(age); //commented because doesn't compile
	}
}
